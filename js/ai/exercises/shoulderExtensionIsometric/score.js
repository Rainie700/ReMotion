export const SH06_ISSUE_LABELS={arm_extension:"手臂向後移動",arm_movement:"手臂位移過多",shrug:"出現聳肩",torso_lean:"身體過度後仰",hip_shift:"骨盆移動"};
export function calculateExtensionIsometricScore(s){let n=100;n-=(s.extensionCount||0)*8;n-=(s.armMovementCount||0)*7;n-=(s.shrugCount||0)*7;n-=(s.torsoLeanCount||0)*10;n-=(s.hipShiftCount||0)*7;n-=Math.max(0,(s.targetReps||0)-(s.totalReps||0))*3;n-=Math.min(10,s.trackingInterruptionCount||0);n=Math.max(0,Math.min(100,Math.round(n)));return{score:n,quality:n>=90?"Excellent":n>=75?"Good":n>=60?"Fair":"Needs Practice"};}
export function buildExtensionIsometricRemark(s){const a=[];if(s.extensionCount)a.push("向後出力但不要讓手臂真的後移");if(s.armMovementCount)a.push("維持等長出力並減少手臂位移");if(s.shrugCount)a.push("放鬆肩膀避免聳肩");if(s.torsoLeanCount)a.push("身體保持直立避免後仰");if(s.hipShiftCount)a.push("骨盆保持中立與穩定");if(s.totalReps<s.targetReps)a.push(`完成 ${s.totalReps}/${s.targetReps} 次 6 秒維持`);return(a.length?a.join("；"):"手臂貼身、肩膀與骨盆穩定")+"。";}
