export const LE07_ISSUE_LABELS={insufficient_lift:"腳跟抬升不足",asymmetry:"左右腳跟抬升不對稱",knee_bend:"膝蓋彎曲",trunk_lean:"身體前傾或偏移",too_fast:"動作速度較快"};
export function calculateCalfRaiseScore(s){let score=100;score-=(s.insufficientLiftCount||0)*8;score-=(s.asymmetryCount||0)*6;score-=(s.kneeBendCount||0)*5;score-=(s.trunkLeanCount||0)*6;score-=(s.tooFastCount||0)*4;score-=Math.max(0,(s.targetReps||0)-(s.totalReps||0))*2;score=Math.max(0,Math.min(100,Math.round(score)));return{score,quality:score>=90?"Excellent":score>=75?"Good":score>=60?"Fair":"Needs Practice"};}
export function buildCalfRaiseRemark(s){const a=[];if(s.insufficientLiftCount)a.push("腳跟可再抬高");if(s.asymmetryCount)a.push("注意左右腳平均出力");if(s.kneeBendCount)a.push("保持膝蓋伸直");if(s.trunkLeanCount)a.push("保持身體垂直");if(s.tooFastCount)a.push("放慢抬起與放下速度");if(s.totalReps<s.targetReps)a.push("完成 "+s.totalReps+"/"+s.targetReps+" 次");return(a.length?a.join("；"):"雙腳踮起穩定，重心控制良好")+"。";}
