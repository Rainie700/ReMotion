export const CR03_ISSUE_LABELS={excessive_tilt:"骨盆傾斜幅度過大",hip_lift:"臀部過度抬起",leg_movement:"腿部代償移動",too_fast:"動作速度過快"};
export function calculatePelvicTiltScore(s){let n=100;n-=(s.excessiveTiltCount||0)*7;n-=(s.hipLiftCount||0)*9;n-=(s.legCompensationCount||0)*8;n-=(s.tooFastCount||0)*5;n-=Math.max(0,(s.targetReps||0)-(s.totalReps||0))*2;n-=Math.min(10,s.trackingInterruptionCount||0);n=Math.max(0,Math.min(100,Math.round(n)));return{score:n,quality:n>=90?"Excellent":n>=75?"Good":n>=60?"Fair":"Needs Practice"};}
export function buildPelvicTiltRemark(s){const a=[];if(s.excessiveTiltCount)a.push("骨盆維持小幅且可控制的傾斜");if(s.hipLiftCount)a.push("臀部不要抬成橋式");if(s.legCompensationCount)a.push("固定雙腿，避免膝蓋帶動");if(s.tooFastCount)a.push("放慢傾斜與回正速度");if(s.totalReps<s.targetReps)a.push(`完成 ${s.totalReps}/${s.targetReps} 次`);return(a.length?a.join("；"):"骨盆傾斜幅度與回正控制良好")+"。";}
