export function calculateBedRollingScore(s){let n=100;n-=(s.shoulderHipSyncCount||0)*6;n-=(s.kneePositionCount||0)*4;n-=(s.tooFastCount||0)*4;n-=(s.tooSlowCount||0)*2;n-=(s.incompleteReturnCount||0)*3;n-=(s.trackingInterruptionCount||0)*2;if(s.totalReps)n-=Math.round((1-(s.validReps||0)/s.totalReps)*10);n=Math.max(0,Math.min(100,Math.round(n)));return{score:n,quality:n>=90?"Excellent":n>=75?"Good":n>=60?"Fair":"Needs Practice"};}
export function buildBedRollingRemark(s){const a=[];if(s.shoulderHipSyncCount)a.push("讓肩膀與骨盆同步轉動");if(s.kneePositionCount)a.push("先屈曲一側膝蓋再帶動翻身");if(s.tooFastCount)a.push("放慢速度避免利用慣性");if(s.incompleteReturnCount)a.push("側躺後完整回到仰躺");if((s.leftReps||0)!==(s.rightReps||0))a.push("左右方向次數盡量平均");return(a.length?a.join("；"):"肩膀與骨盆同步，翻身與回位控制良好")+"。";}
