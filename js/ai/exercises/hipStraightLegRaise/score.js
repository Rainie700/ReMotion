export const HP01_ISSUE_LABELS={insufficient_raise:"抬腿角度不足，請抬至約 30°～45°",excessive_raise:"抬腿過高，避免腰部拱起",knee_bend:"膝蓋保持伸直",body_roll:"固定骨盆，避免身體旋轉",insufficient_hold:"頂端停留約 2 秒再放下",too_fast:"請放慢速度並控制下降"};
export function calculateHipStraightLegRaiseScore(s){let n=100;n-=(s.insufficientRaiseCount||0)*5;n-=(s.excessiveRaiseCount||0)*5;n-=(s.kneeBendCount||0)*6;n-=(s.bodyRollCount||0)*7;n-=(s.insufficientHoldCount||0)*5;n-=(s.tooFastCount||0)*4;n-=Math.max(0,(s.targetReps||0)-(s.totalReps||0))*2;n=Math.max(0,Math.min(100,Math.round(n)));return{score:n,quality:n>=90?"Excellent":n>=75?"Good":n>=60?"Fair":"Needs Practice"};}
export function buildHipStraightLegRaiseRemark(s){const a=[];if(s.insufficientRaiseCount)a.push("抬腿達到約 30°～45°");if(s.excessiveRaiseCount)a.push("避免抬得過高造成腰部代償");if(s.kneeBendCount)a.push("全程保持膝蓋伸直");if(s.bodyRollCount)a.push("收緊核心並固定骨盆");if(s.insufficientHoldCount)a.push("頂端停留約 2 秒");if(s.tooFastCount)a.push("抬起與放下都要緩慢控制");if(s.leftReps<s.targetPerSide||s.rightReps<s.targetPerSide)a.push(`左腿 ${s.leftReps}/${s.targetPerSide}、右腿 ${s.rightReps}/${s.targetPerSide}`);return(a.length?a.join("；"):"抬腿角度、膝蓋伸直與骨盆控制良好")+"。";}
