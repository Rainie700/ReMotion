export function calculateUpperLimbScore(s){const done=Math.min(1,s.totalReps/Math.max(1,s.targetReps));let score=50+50*done-(s.elbowDriftCount+s.tiltCount+s.trunkLeanCount+s.tooFastCount+s.bentElbowCount+s.footMoveCount)*3-s.trackingInterruptionCount*2;score=Math.max(0,Math.min(100,Math.round(score)));return{score,quality:score>=85?"excellent":score>=70?"good":score>=55?"fair":"needs_improvement"};}
export function buildUpperLimbRemark(s){const x=[[s.elbowDriftCount,"固定上臂與手肘位置，避免用肩膀帶動。"],[s.trunkLeanCount,"軀幹保持直立，不要後仰或側傾借力。"],[s.tooFastCount,"放慢速度，抬起與回位都要保持控制。"],[s.bentElbowCount,"抬手時盡量保持手肘伸直。"],[s.footMoveCount,"側向伸取時雙腳需固定，不可跨步。"]].sort((a,b)=>b[0]-a[0]);return x[0][0]?x[0][1]:"上肢動作與回位控制良好，請持續保持。";}
