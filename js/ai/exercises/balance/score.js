export function calculateBalanceScore(s){let score=100;score-=(s.moderateSwayCount||0)*2;score-=(s.largeSwayCount||0)*5;score-=(s.stepCompensationCount||0)*8;score-=(s.trackingInterruptionCount||0)*3;score-=Math.ceil(Math.max(0,(s.targetDurationMs||0)-(s.heldDurationMs||0))/5000)*3;score=Math.max(0,Math.min(100,Math.round(score)));return{score,quality:score>=90?"Excellent":score>=75?"Good":score>=60?"Fair":"Needs Practice"};}
export function buildBalanceRemark(s){const a=[];if(s.largeSwayCount)a.push(`有 ${s.largeSwayCount} 次明顯晃動`);if(s.moderateSwayCount)a.push(`有 ${s.moderateSwayCount} 次中度晃動`);if(s.stepCompensationCount)a.push(`有 ${s.stepCompensationCount} 次疑似跨步補償`);if(s.trackingInterruptionCount)a.push(`有 ${s.trackingInterruptionCount} 次追蹤中斷`);if(!s.completed)a.push(`維持 ${s.heldSeconds}/${s.targetSeconds} 秒`);return(a.length?a.join("；"):"站立期間重心穩定，晃動幅度小")+"。";}
