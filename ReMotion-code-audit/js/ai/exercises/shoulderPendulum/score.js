export const SH01_ISSUE_LABELS={insufficient_swing:"擺動幅度不足",excessive_swing:"擺動幅度過大",active_lift:"手臂主動抬起",trunk_posture:"身體前傾角度需調整",shrug:"出現聳肩",too_fast:"擺動速度較快"};
export function calculateShoulderPendulumScore(s){let n=100;n-=(s.insufficientSwingCount||0)*6;n-=(s.excessiveSwingCount||0)*7;n-=(s.activeLiftCount||0)*8;n-=(s.trunkPostureCount||0)*5;n-=(s.shrugCount||0)*7;n-=(s.tooFastCount||0)*4;n-=Math.max(0,(s.targetReps||0)-(s.totalReps||0))*2;n=Math.max(0,Math.min(100,Math.round(n)));return{score:n,quality:n>=90?"Excellent":n>=75?"Good":n>=60?"Fair":"Needs Practice"};}
export function buildShoulderPendulumRemark(s){const a=[];if(s.activeLiftCount)a.push("讓患側手臂放鬆自然下垂");if(s.excessiveSwingCount)a.push("縮小擺動幅度");if(s.insufficientSwingCount)a.push("可在舒適範圍增加小幅擺動");if(s.trunkPostureCount)a.push("調整身體前傾角度");if(s.shrugCount)a.push("放鬆肩膀避免聳肩");if(s.tooFastCount)a.push("放慢擺動速度");if(s.totalReps<s.targetReps)a.push("完成 "+s.totalReps+"/"+s.targetReps+" 圈");return(a.length?a.join("；"):"手臂保持放鬆，擺動幅度與節奏穩定")+"。";}
