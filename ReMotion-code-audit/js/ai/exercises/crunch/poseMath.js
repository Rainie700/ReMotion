import { CR02_INDEX as P, CR02_THRESHOLDS as T } from "./constants.js";
function ok(p,m){return!!p&&(p.visibility==null||p.visibility>=m)}
function angle(a,b,c){const u={x:a.x-b.x,y:a.y-b.y},v={x:c.x-b.x,y:c.y-b.y},d=Math.hypot(u.x,u.y)*Math.hypot(v.x,v.y);return d?Math.acos(Math.max(-1,Math.min(1,(u.x*v.x+u.y*v.y)/d)))*180/Math.PI:null}
function incline(a,b){let d=Math.abs(Math.atan2(b.y-a.y,b.x-a.x)*180/Math.PI)%180;return d>90?180-d:d}
export function computeCrunchMetrics(l,t=T){if(!l)return{bodyReady:false};const sides=[{name:"left",ear:P.LEFT_EAR,shoulder:P.LEFT_SHOULDER,hip:P.LEFT_HIP,knee:P.LEFT_KNEE},{name:"right",ear:P.RIGHT_EAR,shoulder:P.RIGHT_SHOULDER,hip:P.RIGHT_HIP,knee:P.RIGHT_KNEE}].map(s=>({...s,score:[s.ear,s.shoulder,s.hip,s.knee].filter(i=>ok(l[i],t.MIN_VISIBILITY)).length})).sort((a,b)=>b.score-a.score);const s=sides[0];if(!s||s.score<4)return{bodyReady:false,activeSide:s?.name||null};const ear=l[s.ear],shoulder=l[s.shoulder],hip=l[s.hip],knee=l[s.knee],scale=Math.max(.08,Math.hypot(shoulder.x-hip.x,shoulder.y-hip.y));return{bodyReady:true,activeSide:s.name,trunkInclineDeg:incline(shoulder,hip),hipAngleDeg:angle(shoulder,hip,knee),neckAlignmentDeg:angle(ear,shoulder,hip),hipPoint:{x:hip.x,y:hip.y},bodyScale:scale};}
