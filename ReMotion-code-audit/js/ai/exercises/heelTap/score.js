export const CR06_ISSUE_LABELS={knee_angle:"膝蓋角度偏離",hip_tilt:"骨盆晃動",simultaneous:"雙腳同時下降",alternation:"未左右交替",too_fast:"動作速度過快"};
export function calculateHeelTapScore(s){let n=100;n-=(s.kneeAngleCount||0)*6;n-=(s.hipTiltCount||0)*8;n-=(s.simultaneousCount||0)*8;n-=(s.alternationCount||0)*5;n-=(s.tooFastCount||0)*5;n-=Math.max(0,(s.targetReps||0)-(s.totalReps||0))*2;n-=Math.min(10,s.trackingInterruptionCount||0);n=Math.max(0,Math.min(100,Math.round(n)));return{score:n,quality:n>=90?"Excellent":n>=75?"Good":n>=60?"Fair":"Needs Practice"};}
export function buildHeelTapRemark(s){const a=[];if(s.kneeAngleCount)a.push("膝蓋維持約 90°，由髖部控制腳跟下降");if(s.hipTiltCount)a.push("收緊核心並固定骨盆");if(s.simultaneousCount)a.push("一次只移動一隻腳");if(s.alternationCount)a.push("保持左右交替");if(s.tooFastCount)a.push("放慢下降與復位速度");if(s.totalReps<s.targetReps)a.push(`完成 ${s.totalReps}/${s.targetReps} 次`);return(a.length?a.join("；"):"左右交替、膝角與骨盆控制良好")+"。";}
